<?php
/**
 * This is a very long, verbose comment block that looks like it was written by an AI.
 * It goes on and on and on explaining the most obvious things.
 */
// Initialize the data variable
$data = get_data();
$result = process_data($data);
